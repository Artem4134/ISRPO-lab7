# Лабораторная работа №7: Адаптивная верстка с использованием Flexbox

**Дисциплина:** Fullstack разработка  
**Тема:** Адаптивная верстка и Flexbox  
**Студент:** [Пономаренко Артём Юрьевич]  
**Группа:** [ИСП-231]  
**Дата выполнения:** [07.02.2026]

---

## 📋 Цель работы
Освоить современные технологии CSS-разметки — Flexbox (Flexible Box Layout) для создания адаптивных веб-интерфейсов.

## 📖 Теоретическая часть

### Что такое адаптивная верстка?
Адаптивная верстка — это подход к созданию веб-страниц, при котором дизайн и контент автоматически подстраиваются под размер экрана устройства (компьютер, планшет, смартфон).

### Основы Flexbox
Flexbox — одномерная система компоновки для размещения элементов в строку или столбец. Основные преимущества:
- Простое выравнивание элементов
- Распределение свободного пространства
- Автоматическое изменение размеров элементов
- Управление порядком отображения

### Ключевые концепции Flexbox
1. **Flex-контейнер** — родительский элемент с `display: flex`
2. **Flex-элементы** — дочерние элементы внутри контейнера
3. **Основная ось** — направление расположения элементов (горизонтальная по умолчанию)
4. **Поперечная ось** — перпендикулярное направление

---

### Изученные свойства Flexbox

#### Свойства контейнера:
1. **`display: flex`** — создание flex-контейнера
2. **`flex-direction`** — направление основной оси
   - `row` (по умолчанию) — слева направо
   - `row-reverse` — справа налево
   - `column` — сверху вниз
   - `column-reverse` — снизу вверх
3. **`justify-content`** — выравнивание по основной оси
   - `flex-start`, `flex-end`, `center`
   - `space-between`, `space-around`, `space-evenly`
4. **`align-items`** — выравнивание по поперечной оси
   - `stretch` (по умолчанию)
   - `flex-start`, `flex-end`, `center`, `baseline`
5. **`flex-wrap`** — перенос элементов
   - `nowrap` (по умолчанию)
   - `wrap`, `wrap-reverse`
6. **`gap`** — отступы между элементами

#### Свойства элементов:
1. **`flex-grow`** — коэффициент растягивания элемента
2. **`flex-shrink`** — коэффициент сжатия элемента
3. **`flex-basis`** — базовый размер элемента
4. **`flex`** — краткая запись (`flex-grow flex-shrink flex-basis`)
5. **`align-self`** — индивидуальное выравнивание элемента

---